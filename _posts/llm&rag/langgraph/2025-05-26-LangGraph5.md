---
title: AgentWorkflow
description: Let’s Understanding `Agent Workflow` in LangGraph.
author: DS2Man
date: 2025-05-26 11:00:00 +0000
categories: [LLM&RAG, L&R-LangGraph]
tags:
  - RAG
  - LangGraph
  - LangChain
math: true
pin: true
---

An **Agent Workflow** leverages the built-in agent systems provided by LangGraph, such as the [create_react_agent()](https://langchain-ai.github.io/langgraph/reference/agents/?h=create_react_agent#langgraph.prebuilt.chat_agent_executor.create_react_agent) functions.    
Through the previous post, we covered the basic concepts, and in this post, we’ll write the actual code.    
There are five main steps involved. The same applies to the Agent-like Workflow. The key difference is that, in the Agent-like Workflow, you manually construct the agent’s behavior by separately defining the Tool Node and the LLM with tools Node and connecting them directly within LangGraph. We’ll explore this in more detail in the next post.

<!--
`Agent Workflow`는 LangChain(LangGraph)에서 제공하는 **공식 에이전트 구성 방식**을 기반으로 합니다. 대표적인 예는 create_react_agent()함수입니다.

이전 포스트를 통해서 기본적인 내용은 파악했고, 이번 포스트에서는 구체적으로 코드를 작성해보겠다.

크게 5단계로 되어있다. Agent-like Workflow 도 동일하다. 차이가 있다면 Agent-like Workflow 는 Agent 역활을 하는 부분을 직접 Tool Node와 LLM with tools Node 를 구분해서 직접 Langgraph에 연결하는 부분이다. 다음 포스트에서 자세히 배우겠다.
-->

1. Define the Required Tools for Nodes
2. Define the State
3. Define the Nodes
4. Define Conditional Edges
5. Construct the Graph

## *1. Define the Required Tools for Nodes*

### *Tools*

Tools that assist the LLM when additional capabilities are needed.
Tools에 대한 정의는 이전 포스트([LangChain-Agent](/posts/LangChain-Tools2/))를 참고해라

```python
from ai.tools.get_time import get_current_datetime_tool
from ai.tools.repl import python_repl_tool
from ai.tools.search_web import googlenews_search_tool, tavily_search_tool
from ai.tools.retriever import pdf_retriever_tool  

my_tools = [python_repl_tool, tavily_search_tool, googlenews_search_tool, get_current_datetime_tool, pdf_retriever_tool]
```

###  *Agent*

Define the agent using `create_react_agent`.

```python
llm = ChatOpenAI(
    model=openai_api_model,
    openai_api_base=openai_api_base,
    openai_api_key=openai_api_key
)

agent = create_react_agent(
    model=llm,
    tools=my_tools,
    state_modifier="You are a helpful assistant"
)
```

## *2. Define the State*

```python
class GraphState(TypedDict):
    # The line below technically works, but since it's just defined as a list,
    # it's unclear whether it's a list of int, str, or dict.
    # IDEs or tools like mypy that rely on type hints may not provide accurate warnings.
    # messages: Annotated[list, add_messages]
    messages: Annotated[Sequence[BaseMessage], add_messages]
```

## *3. Define the Nodes*

```python
def run_agent(state: GraphState) -> GraphState:
    response = agent.invoke({"messages": state["messages"]})
```

## *4. Define Conditional Edges*

In Step 3, we used `create_react_agent()` to connect the LLM with `my_tools`. As a result, implementing conditional branching logic isn't necessary. However, we implemented `tools_condition` manually for reference. Just take it as a reference.

```python
def my_tools_condition(
    state: GraphState,
):
    if messages := state.get("messages", []):
        # Extract the most recent AI message
        ai_message = messages[-1]
    else:
        # Raise an exception if there are no messages in the input state
        raise ValueError(f"No messages found in input state to tool_edge: {state}")

    # If the AI message contains a tool call, return "tools"
    if hasattr(ai_message, "tool_calls") and len(ai_message.tool_calls) > 0:
        # Tool call exists, return "tools"
        return "tools"
    # If no tool call exists, return "END"
    return END
```

## *5. Construct the Graph*

```python
# Construct the Graph
workflow = StateGraph(GraphState)
workflow.add_node("MyAgent", run_agent)

workflow.set_entry_point("MyAgent")
workflow.add_edge("MyAgent", END)

# Checkpoint configuration (memory storage)
# memory = MemorySaver()
os.makedirs("./cache", exist_ok=True)
conn = sqlite3.connect("./cache/checkpoints.sqlite", check_same_thread=False)
memory =  SqliteSaver(conn)
agent_app = workflow.compile(checkpointer=memory)

# Graph visualization
visualize_graph(agent_app, xray=True)
```

![Agent Workflow](/assets/img/langgraph/2025-05-24-LangGraph1_1.png)
_Agent Workflow_


## *Execution and evaluation*

### *invoke*

```python
config = RunnableConfig(recursion_limit=10, configurable={"thread_id": random_uuid()})

question = "오늘의 AI 뉴스 알려줘."
inputs = {
    "messages": [
        HumanMessage(
            content=question
        )
    ],
}
result = agent_app.invoke(inputs, config)

print("====" * 4)
print(f"Total number of messages: {len(result['messages'])}")
print("====" * 4)
print("All messages:")
for idx in range(len(result['messages'])):
    print(f"{result['messages'][idx]}")
print("====" * 4)
print(f"Final response:\n{result['messages'][-1].content}")
print("====" * 4)
```

```bash
================
Total number of messages: 4
================
All messages:
content='오늘의 AI 뉴스 알려줘.' additional_kwargs={} response_metadata={} id='f1c11d3e-4c86-4bd0-af4a-74581a0c17b5'
content='' additional_kwargs={'tool_calls': [{'id': '40bvehr93', 'function': {'arguments': '{"query":"AI 뉴스"}', 'name': 'googlenews_search_tool'}, 'type': 'function', 'index': 0}], 'refusal': None} response_metadata={'token_usage': {'completion_tokens': 27, 'prompt_tokens': 978, 'total_tokens': 1005, 'completion_tokens_details': None, 'prompt_tokens_details': None}, 'model_name': 'meta-llama/llama-4-maverick', 'system_fingerprint': 'fp_475b3d8b3e', 'finish_reason': 'tool_calls', 'logprobs': None} id='run--524f2437-29ce-4e98-b142-47139f2ed725-0' tool_calls=[{'name': 'googlenews_search_tool', 'args': {'query': 'AI 뉴스'}, 'id': '40bvehr93', 'type': 'tool_call'}] usage_metadata={'input_tokens': 978, 'output_tokens': 27, 'total_tokens': 1005, 'input_token_details': {}, 'output_token_details': {}}
content='[{"url": "https://news.google.com/rss/articles/CBMiakFVX3lxTE1EalUxcEYzTk5qU2JFYVZXaE43eWIzdHpFY1lJdjJxdFJ0enpaa2hONk9sNGsyaXFqTWZ1UmxuemNVbVM5cUYzV0o5S1RsRXplaC1lZWRwMEpEcjFJVldkbkNtb0RybnV6dlE?oc=5", "content": "LG, 하이브리드 AI ‘엑사원 4.0’ 공개...\\"글로벌 프론티어 성능 도달\\" - AI타임스"}, {"url": "https://news.google.com/rss/articles/CBMiTkFVX3lxTE1tY1NiYW1lalA1UTVYQm1vM0xtNzJzRTM1YUkwRGwwZGJ2QnVvWjdsVXRVbWNOS2R3SnFyazJPelc0d09iZTFwT1dEbEhFUQ?oc=5", "content": "“미리 배워둘걸”…천문학적인 비용 아끼지 않는 빅테크들, AI인재 쟁탈전 - 매일경제"}, {"url": "https://news.google.?oc=5", "content": "젠슨 황 \\"中 AI모델들 월드클래스…친구들과 손잡고 미래 열것\\" - 연합뉴스"}]' name='googlenews_search_tool' id='fb8b87a6-76ea-46bc-b7d8-54e6f7c68579' tool_call_id='40bvehr93'
content="최근 AI 관련 주요 뉴스는 다음과 같습니다:\n\n1. LG가 하이브리드 AI '엑사원4.0'을 공개하며 글로벌 프론티어 성능에 도달했다고 발표했습니다.\n2. 빅테크 기업들이 천문학적인 비용을 들여 AI 인재를 영입하는 경쟁을 벌이고 있습니다.\n3. 젠슨 황 엔비디아 CEO는 중국 AI 모델들이 월드클래스 수준이라고 평가하며, 중국 기업들과 협력하여 미래를 열 것이라고 언급했습니다.\n\n자세한 내용은 관련 기사를 참고하시기 바랍니다." additional_kwargs={'refusal': None} response_metadata={'token_usage': {'completion_tokens': 110, 'prompt_tokens': 1516, 'total_tokens': 1626, 'completion_tokens_details': None, 'prompt_tokens_details': None}, 'model_name': 'meta-llama/llama-4-maverick', 'system_fingerprint': 'fp_565109a0df', 'finish_reason': 'stop', 'logprobs': None} id='run--59e0e546-cfa3-4333-a2e8-d5aa8c943fee-0' usage_metadata={'input_tokens': 1516, 'output_tokens': 110, 'total_tokens': 1626, 'input_token_details': {}, 'output_token_details': {}}
================
최종 답변:
최근 AI 관련 주요 뉴스는 다음과 같습니다:

1. LG가 하이브리드 AI '엑사원4.0'을 공개하며 글로벌 프론티어 성능에 도달했다고 발표했습니다.
2. 빅테크 기업들이 천문학적인 비용을 들여 AI 인재를 영입하는 경쟁을 벌이고 있습니다.
3. 젠슨 황 엔비디아 CEO는 중국 AI 모델들이 월드클래스 수준이라고 평가하며, 중국 기업들과 협력하여 미래를 열 것이라고 언급했습니다.

자세한 내용은 관련 기사를 참고하시기 바랍니다.
================
```

### *stream*

LangGraph provides three `stream_mode` options, each returning different output formats depending on the selected mode:
- `messages`: returns a `tuple`    
- `values`: returns a `dict`    
- `update` (default): returns a `dict`

When using an agent created via `create_react_agent`, internal results are not streamed or displayed by default.

#### *stream_mode="messages"*

When using `stream_mode="messages", you can filter and extract messages from specific nodes using `metadata["langgraph_node"]`.

```python
config = RunnableConfig(recursion_limit=10, configurable={"thread_id": random_uuid()})

question = "오늘의 AI 뉴스 알려줘."
inputs = {
    "messages": [
        HumanMessage(
            content=question
        )
    ],
}

for chunk_msg, metadata in agent_app.stream(inputs, config, stream_mode="messages"):
    # if isinstance(chunk_msg, AIMessageChunk):
    #     print(chunk_msg.content, end="", flush=True)
    if metadata["langgraph_node"] == "MyAgent":
        print(chunk_msg.content, end="", flush=True)
```

```bash
최근 AI 뉴스는 LG가 하이브리드 AI '엑사원4.0'을 공개하고, 코오롱베니트가 LG 엑사원 기반 AI 관제 패키지를 개발하는 등 다양한 분야에서 인공지능 기술이 발전하고 있습니다. 자세한 내용은 관련 기사를 참고하시기 바랍니다.
```

#### *stream_mode="values"*

```python
config = RunnableConfig(recursion_limit=10, configurable={"thread_id": random_uuid()})

question = "오늘의 AI 뉴스 알려줘."
inputs = {
    "messages": [
        HumanMessage(
            content=question
        )
    ],
}

for step in agent_app.stream(inputs, config, stream_mode="values"):
    # print(f"Step Output: {step}")
    for key, value in step.items():
        print(f"\n[ {key} ]")
        value["messages"][-1].pretty_print()
```

```bash
```

#### *stream_mode="updates"*

```python
config = RunnableConfig(recursion_limit=10, configurable={"thread_id": random_uuid()})

question = "오늘의 AI 뉴스 알려줘."
inputs = {
    "messages": [
        HumanMessage(
            content=question
        )
    ],
}

for step in agent_app.stream(inputs, config, stream_mode="updates"):
    # print(f"Step Output: {step}")
    for key, value in step.items():
        print(f"\n[ {key} ]")
        value["messages"][-1].pretty_print()

```

```bash
```